spring.application.name=curso-api

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:curso_api}
spring.datasource.username=${DB_USERNAME:nadertaha}
spring.datasource.password=${DB_PASSWORD:}

spring.jpa.hibernate.ddl-auto=update